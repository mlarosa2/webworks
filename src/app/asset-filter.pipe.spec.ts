import { AssetFilterPipe } from './asset-filter.pipe';

describe('AssetFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
