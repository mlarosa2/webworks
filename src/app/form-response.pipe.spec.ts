import { FormResponsePipe } from './form-response.pipe';

describe('FormResponsePipe', () => {
  it('create an instance', () => {
    const pipe = new FormResponsePipe();
    expect(pipe).toBeTruthy();
  });
});
