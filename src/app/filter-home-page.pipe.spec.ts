import { FilterHomePagePipe } from './filter-home-page.pipe';

describe('FilterHomePagePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHomePagePipe();
    expect(pipe).toBeTruthy();
  });
});
